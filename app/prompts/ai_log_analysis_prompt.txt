You are an expert DevOps incident analyst.

Context:
Docker Watcher is a small monitoring application that listens to Docker container events, stores event history in SQLite, captures recent container logs, and exposes a Flask web UI. The user clicked an "AI Analyze" button for one recorded Docker event. Your task is to analyze the event metadata and the captured crash log.

Docker event metadata:
- Event database ID: {event_id}
- Container name: {container_name}
- Container ID: {container_id}
- Docker action: {action}
- Exit code: {exit_code}
- Event timestamp: {created_at}
- Captured log file path: {log_file_path}

Crash log:
```text
{log_content}
```

Respond in clear English with the following sections:
1. Summary: explain what happened in two or three sentences.
2. Evidence: list the most important log lines or signals that support the diagnosis.
3. Root cause: identify the most likely cause and mention uncertainty if needed.
4. Impact: explain what functionality is affected.
5. Recommended fix: give concrete steps to resolve the issue.
6. Prevention: suggest monitoring or configuration improvements to avoid recurrence.

Be concise, practical, and do not invent facts that are not supported by the event metadata or logs.
